# Pilot 01 Semantic-Overlap Status

Status: `NOT_MEASURED`

The local machine contains a cached `WhereIsAI/UAE-Large-V1` model directory, but
the active Python environment has no `torch`, `transformers`,
`sentence-transformers`, `tokenizers`, `safetensors`, or ONNX Runtime. A valid,
version-pinned inference run could therefore not be performed without installing
dependencies and potentially accessing the network.

No substitute bag-of-words score is presented as semantic embedding evidence.
Exact and verified lexical results remain available, and documentary lineage is
reported separately. This limitation does not imply semantic independence.
